import WhyAbaPage from "../WhyAbaPage";

export const metadata = {
  title: "Why ABA — We Clean ABA",
  description:
    "Every ABA operator manages variables they can't control. We became obsessed with one of the few they can: the environment.",
};

export default function WhyAba() {
  return <WhyAbaPage />;
}
